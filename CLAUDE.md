# H2OMoney - Sui Vibe Hackathon 2026

## 專案概述
H2OMoney 是一款基於 Sui 區塊鏈的 Telegram 智能定投機器人（Smart DCA Bot），整合 Cetus 聚合器和 stablelayer SDK，為用戶提供自動化的 DeFi 定投與收益優化服務。

**參賽賽道：** Cetus + stablelayer（雙賽道）

---

## H2O Smart DCA - 會賺錢的定投機器人

### 核心概念

> **傳統 DCA 的問題：等待買入的錢是死錢**

```
傳統 DCA:
存入 400 USDC → 等待 4 週 → 每週買 100 USDC 的 SUI
問題：300 USDC 在等待期間完全沒收益 ❌

H2O Smart DCA:
存入 400 USDC → 自動轉 H2OUSD 生息 → 每週取出 100 買 SUI
等待期間的錢也在賺利息 ✅
```

### 產品願景
> 「像水一樣靈活，像複利一樣強大」

H2O Smart DCA 不只定投，還會讓閒置資金自動生息，實現「邊等待、邊賺錢」的智能理財。

---

## 雙層收益架構（自動生息金庫）

### 運作原理

```
用戶存入 100 USDC
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│              自動生息金庫 (Yield Vault)                   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│   ┌─────────────────────────────────────────────────┐   │
│   │     第一層：BrandUSD 底層收益                     │   │
│   │     ─────────────────────────────                │   │
│   │     USDC → mint BrandUSD (會自己長大的穩定幣)     │   │
│   │     • Bucket Protocol 底層抵押資產收益            │   │
│   │     • 預估 APY: 3-5%                             │   │
│   └─────────────────────────────────────────────────┘   │
│                         │                               │
│                         ▼                               │
│   ┌─────────────────────────────────────────────────┐   │
│   │     第二層：Cetus 窄區間 LP 手續費                 │   │
│   │     ─────────────────────────────                │   │
│   │     BrandUSD 投入 Cetus 集中流動性池              │   │
│   │     • 窄區間策略 (非傳統 V2 寬區間)               │   │
│   │     • 集中流動性 = 更高資本效率                   │   │
│   │     • 預估 APY: 5-15% (依交易量)                 │   │
│   └─────────────────────────────────────────────────┘   │
│                                                         │
│   總預估 APY: 8-20%                                     │
└─────────────────────────────────────────────────────────┘
```

### 自動重置機制 (Auto Rebalance)

當 BrandUSD 因底層收益而增值時（如 1 BrandUSD = 1.05 USDC），會導致：
1. **價格偏移**：LP 位置偏離最佳區間
2. **自動重置**：金庫機器人自動撤出並重新部署到「手續費最肥」的區間
3. **收益最大化**：確保資金永遠待在最優區間

```
BrandUSD 增值 → 價格偏移 → 觸發 Rebalance → 重新定位區間 → 繼續賺手續費
```

### 套利循環引擎（流動性引擎）

這是維持整個系統活力的「外部動力」：

```
┌────────────────────────────────────────────────────────┐
│                   套利循環機制                          │
├────────────────────────────────────────────────────────┤
│                                                        │
│  1. 脫鉤觸發                                           │
│     隨著收益累積，BrandUSD 內在價值 > 1 USDC           │
│     例：1 BrandUSD = 1.05 USDC                        │
│                         │                              │
│                         ▼                              │
│  2. 吸引套利                                           │
│     價差吸引外部套利機器人進場                          │
│                         │                              │
│                         ▼                              │
│  3. 產生交易                                           │
│     套利者買賣行為 = 交易量                             │
│                         │                              │
│                         ▼                              │
│  4. 回饋金庫                                           │
│     更多交易量 = 金庫 LP 賺更多手續費                   │
│                         │                              │
│                         ▼                              │
│  5. 正向循環                                           │
│     收益增加 → BrandUSD 增值 → 更多套利 → 更多手續費    │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## H2O Smart DCA 運作流程

```
┌──────────────────────────────────────────────────────────────┐
│  步驟 1：用戶設定策略                                         │
│  「每週用 $100 買 SUI，連續 4 週」                            │
│  存入 400 USDC                                               │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  步驟 2：資金進入自動生息金庫                                  │
│  400 USDC → mint 400 H2OUSD (BrandUSD)                       │
│  年化 ~8-20% 持續累積                                         │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  步驟 3：每週自動執行                                         │
│                                                              │
│    H2OUSD burn → 100 USDC                                    │
│         │                                                    │
│         ▼                                                    │
│    Cetus Aggregator 找最佳價格                                │
│         │                                                    │
│         ▼                                                    │
│    買入 SUI 發送到用戶錢包                                    │
│                                                              │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│  步驟 4：結果                                                 │
│  買到的 SUI + 等待期間賺的利息                                │
│  比傳統 DCA 多賺 ~2-5% 額外收益                               │
└──────────────────────────────────────────────────────────────┘
```

---

## 技術整合

| 功能 | SDK | 說明 |
|------|-----|------|
| 生息等待池 | StableLayer SDK | mint/burn H2OUSD |
| 最佳價格交易 | Cetus Aggregator | 多 DEX 路由尋找最優價格 |
| 定時執行 | Cetus DCA SDK 或自建排程 | 自動化執行定投 |
| 限價 DCA | Cetus Limit SDK | 價格到才買 |
| 集中流動性 | Cetus CLMM SDK | 窄區間 LP 管理 |

---

## 進階功能

### 1. 智能限價 DCA
```
「每週買 $100 SUI，但只在價格低於 7 日均價時買」
→ 等待期間錢在生息
→ 價格到了自動買入
→ 沒到就繼續等，繼續賺利息
```

### 2. 多幣種定投
```
「每月 $300 定投：50% SUI, 30% CETUS, 20% DEEP」
→ 全部資金先存 H2OUSD
→ 按比例分批買入不同幣種
```

### 3. 止盈自動賣出
```
「SUI 漲 50% 自動賣掉換回 H2OUSD 繼續生息」
→ 形成完整的投資循環
```

### 4. 收益複投
```
「定投收益自動加入本金」
→ 複利效應最大化
```

---

## 系統架構

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Layer                              │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐    ┌─────────────────┐                     │
│  │  Telegram Bot   │    │   Web dApp      │                     │
│  │  (Grammy)       │    │   (Next.js)     │                     │
│  └────────┬────────┘    └────────┬────────┘                     │
│           │                      │                              │
├───────────┴──────────────────────┴──────────────────────────────┤
│                      Application Layer                          │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                    DCA Engine (Node.js)                     ││
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌─────────────┐ ││
│  │  │ Scheduler │ │  Strategy │ │  Rebalance│ │   Wallet    │ ││
│  │  │  Manager  │ │  Engine   │ │  Manager  │ │   Manager   │ ││
│  │  └───────────┘ └───────────┘ └───────────┘ └─────────────┘ ││
│  └─────────────────────────────────────────────────────────────┘│
│                              │                                  │
├──────────────────────────────┼──────────────────────────────────┤
│                      Protocol Layer                             │
├──────────────────────────────┼──────────────────────────────────┤
│                              │                                  │
│  ┌──────────────┐    ┌───────┴───────┐    ┌──────────────┐     │
│  │    Cetus     │    │  H2O Yield    │    │  StableLayer │     │
│  │  Aggregator  │◄───│    Vault      │───►│  (BrandUSD)  │     │
│  │  (最佳路徑)   │    │   (Move)      │    │   (生息)     │     │
│  └──────────────┘    └───────────────┘    └──────────────┘     │
│         │                    │                    │             │
│         │            ┌───────┴───────┐            │             │
│         │            │  Cetus CLMM   │            │             │
│         └───────────►│  (窄區間 LP)  │◄───────────┘             │
│                      └───────────────┘                          │
│                              │                                  │
├──────────────────────────────┼──────────────────────────────────┤
│                       Sui Blockchain                            │
└──────────────────────────────────────────────────────────────────┘
```

---

## 智能合約架構 (Move)

```
contracts/
├── Move.toml
└── sources/
    ├── h2o_vault.move           # 主金庫合約
    │   ├── create_vault         # 建立金庫
    │   ├── deposit              # 存入 USDC
    │   ├── withdraw             # 提取資金
    │   └── get_vault_info       # 查詢金庫狀態
    │
    ├── dca_position.move        # DCA 倉位管理
    │   ├── create_position      # 建立定投倉位
    │   ├── execute_dca          # 執行定投
    │   ├── pause_position       # 暫停倉位
    │   ├── resume_position      # 恢復倉位
    │   └── close_position       # 關閉倉位
    │
    ├── yield_optimizer.move     # 收益優化器
    │   ├── mint_brand_usd       # 換成 BrandUSD
    │   ├── burn_brand_usd       # 贖回 USDC
    │   ├── add_clmm_liquidity   # 加入窄區間 LP
    │   ├── remove_liquidity     # 移除流動性
    │   └── rebalance            # 自動重置區間
    │
    ├── strategy.move            # 定投策略模組
    │   ├── fixed_amount         # 固定金額策略
    │   ├── limit_price          # 限價策略
    │   ├── value_averaging      # 價值平均策略
    │   └── multi_token          # 多幣種策略
    │
    └── keeper.move              # 自動執行管理
        ├── register_task        # 註冊定時任務
        ├── execute_pending      # 執行待處理任務
        ├── trigger_rebalance    # 觸發重置
        └── calculate_reward     # 計算 keeper 獎勵
```

---

## 資料模型

```typescript
// DCA 倉位
interface DCAPosition {
  id: string;                    // 倉位 ID
  owner: string;                 // 擁有者地址

  // 資金配置
  sourceToken: string;           // 投入代幣 (USDC)
  targetTokens: TokenAllocation[]; // 目標代幣配置

  // 定投參數
  amountPerPeriod: bigint;       // 每期金額
  intervalSeconds: number;       // 間隔秒數
  totalPeriods: number;          // 總期數
  executedPeriods: number;       // 已執行期數
  nextExecutionTime: number;     // 下次執行時間

  // 策略設定
  strategy: StrategyType;
  limitPrice?: number;           // 限價 (可選)
  priceOracle?: string;          // 價格預言機

  // 金庫收益
  vaultBalance: bigint;          // 金庫餘額 (H2OUSD)
  accumulatedYield: bigint;      // 累計收益
  clmmPositionId?: string;       // CLMM LP 位置 ID

  // 統計
  totalInvested: bigint;
  totalAcquired: bigint;
  averagePrice: bigint;

  // 狀態
  status: 'active' | 'paused' | 'completed';
  createdAt: number;
  updatedAt: number;
}

// 代幣配置
interface TokenAllocation {
  token: string;                 // 代幣地址
  symbol: string;                // 代幣符號
  percentage: number;            // 佔比 (1-100)
}

// 策略類型
enum StrategyType {
  FIXED = 'fixed',               // 固定金額
  LIMIT = 'limit_price',         // 限價買入
  VALUE_AVG = 'value_averaging', // 價值平均
  MULTI_TOKEN = 'multi_token'    // 多幣種
}

// 金庫狀態
interface VaultState {
  totalDeposited: bigint;        // 總存入
  totalH2OUSD: bigint;           // H2OUSD 餘額
  clmmLiquidity: bigint;         // CLMM 流動性
  pendingYield: bigint;          // 待領收益
  lastRebalanceTime: number;     // 上次重置時間
  currentAPY: number;            // 當前 APY
}
```

---

## Telegram Bot 指令

| 指令 | 功能 | 說明 |
|------|------|------|
| `/start` | 啟動機器人 | 歡迎訊息與功能介紹 |
| `/connect` | 連接錢包 | 綁定 Sui 錢包 |
| `/new` | 建立新定投 | 互動式設定定投策略 |
| `/list` | 查看所有倉位 | 列出活躍的定投倉位 |
| `/status <id>` | 查看倉位詳情 | 顯示單一倉位完整資訊 |
| `/yield` | 查看收益 | 顯示生息收益統計 |
| `/pause <id>` | 暫停定投 | 暫停指定倉位 |
| `/resume <id>` | 恢復定投 | 恢復暫停的倉位 |
| `/close <id>` | 關閉倉位 | 關閉並提取資金 |
| `/settings` | 設定 | 通知偏好等設定 |
| `/help` | 幫助 | 指令說明 |

### Bot 互動流程範例

```
用戶: /new
Bot:  🌊 建立新的 Smart DCA 倉位

      選擇定投策略：
      1. 固定金額 - 每期投入相同金額
      2. 智能限價 - 只在目標價格以下買入
      3. 多幣種 - 同時定投多個代幣

用戶: 1
Bot:  選擇目標代幣：
      • SUI
      • CETUS
      • DEEP
      • 自訂代幣地址

用戶: SUI
Bot:  設定每期投入金額（USDC）：

用戶: 100
Bot:  選擇定投週期：
      • 每日
      • 每週
      • 每兩週
      • 每月

用戶: 每週
Bot:  定投幾期？（輸入期數）

用戶: 4
Bot:  📊 確認你的 Smart DCA 設定：

      • 策略：固定金額
      • 投入：100 USDC × 4 期 = 400 USDC
      • 目標：SUI
      • 週期：每週

      💰 收益優化已自動啟用：
      • 等待期間資金存入生息金庫
      • 預估額外收益：~8-20% APY

      確認建立？[是/否]

用戶: 是
Bot:  ✅ Smart DCA 倉位已建立！

      📋 倉位 ID: h2o_dca_abc123

      🔄 運作流程：
      1. 400 USDC 已轉換為 H2OUSD
      2. 資金已投入生息金庫
      3. 每週自動執行定投

      ⏰ 下次執行：2026-02-11 00:00 UTC

      使用 /status h2o_dca_abc123 查看詳情
```

---

## WebApp 頁面結構

```
webapp/
├── src/
│   ├── app/
│   │   ├── page.tsx                 # 首頁 - 產品介紹
│   │   ├── layout.tsx               # 全局佈局
│   │   ├── dashboard/
│   │   │   └── page.tsx             # 儀表板 - 倉位總覽
│   │   ├── position/
│   │   │   └── [id]/page.tsx        # 倉位詳情頁
│   │   ├── create/
│   │   │   └── page.tsx             # 建立定投頁
│   │   ├── yield/
│   │   │   └── page.tsx             # 收益分析頁
│   │   └── docs/
│   │       └── page.tsx             # 文檔說明頁
│   │
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Header.tsx
│   │   │   ├── Footer.tsx
│   │   │   └── Sidebar.tsx
│   │   ├── wallet/
│   │   │   └── WalletConnect.tsx    # 錢包連接
│   │   ├── dca/
│   │   │   ├── PositionCard.tsx     # 倉位卡片
│   │   │   ├── CreateDCAForm.tsx    # 建立表單
│   │   │   ├── DCAChart.tsx         # 定投走勢圖
│   │   │   └── ExecutionHistory.tsx # 執行歷史
│   │   └── yield/
│   │       ├── YieldStats.tsx       # 收益統計
│   │       ├── APYDisplay.tsx       # APY 顯示
│   │       └── RebalanceLog.tsx     # 重置紀錄
│   │
│   ├── hooks/
│   │   ├── useDCAPosition.ts        # 倉位 Hook
│   │   ├── useVaultState.ts         # 金庫狀態 Hook
│   │   ├── useYieldStats.ts         # 收益 Hook
│   │   └── usePriceData.ts          # 價格 Hook
│   │
│   └── lib/
│       ├── sui-client.ts            # Sui SDK 封裝
│       ├── cetus-sdk.ts             # Cetus SDK 封裝
│       └── stablelayer-sdk.ts       # StableLayer SDK 封裝
```

---

## 技術棧

### 區塊鏈層
- **Sui Move 2024** - 智能合約開發（必須使用最新語法）
- **Sui SDK (TypeScript)** - 最新版本，不使用 deprecated API
- **Cetus Aggregator** - https://github.com/CetusProtocol/aggregator
- **Cetus SDK v2** - https://github.com/CetusProtocol/cetus-sdk-v2
- **Cetus CLMM SDK** - 集中流動性做市
- **stablelayer SDK** - https://github.com/StableLayer/stable-layer-sdk

### 應用層
- **Telegram Bot API** - Grammy 框架
- **TypeScript** - 主要開發語言
- **Node.js** - 運行環境
- **node-cron** - 定時任務排程

### 前端（dApp 展示頁）
- **Next.js 14** - React 框架 (App Router)
- **@mysten/dapp-kit** - Sui 錢包連接
- **TailwindCSS** - 樣式框架
- **Recharts** - 圖表庫

---

## 開發規範

### Move 合約
- 使用 Move 2024 edition 語法
- 合約部署到 Sui testnet/mainnet
- 遵循 Sui Move 最佳實踐

### TypeScript
- 嚴格模式 (strict: true)
- 使用 ESM 模組
- 錯誤處理必須完整

### 代碼風格
- 變數/函數：camelCase
- 類型/介面：PascalCase
- 常數：UPPER_SNAKE_CASE
- 檔案名：kebab-case

### Git Commit 風格
使用 Conventional Commits 格式：`<type>: <description>`

| Type | 用途 | 範例 |
|------|------|------|
| `feat` | 新增功能 | `feat: 新增套利計算功能` |
| `fix` | 修復 bug | `fix: 解決錢包連接失敗 bug` |
| `docs` | 文件更新 | `docs: 更新 README 安裝說明` |
| `style` | 格式調整（不影響邏輯） | `style: 統一縮排格式` |
| `refactor` | 重構（不新增功能或修 bug） | `refactor: 重構交易模組` |
| `test` | 測試相關 | `test: 新增單元測試` |
| `chore` | 建置/工具/依賴 | `chore: 更新依賴版本` |

---

## 專案結構
```
H2OMoney/
├── CLAUDE.md                    # 此配置文件
├── README.md                    # 專案說明（部署和運行指南）
├── contracts/                   # Sui Move 智能合約
│   ├── sources/
│   │   ├── h2o_vault.move
│   │   ├── dca_position.move
│   │   ├── yield_optimizer.move
│   │   ├── strategy.move
│   │   └── keeper.move
│   └── Move.toml
├── bot/                         # Telegram Bot
│   ├── src/
│   │   ├── index.ts             # 入口
│   │   ├── bot.ts               # Bot 初始化
│   │   ├── commands/            # 指令處理
│   │   ├── services/            # 業務邏輯
│   │   ├── scheduler/           # 定時任務
│   │   └── utils/               # 工具函數
│   └── package.json
├── webapp/                      # dApp 展示頁面
│   ├── src/
│   │   ├── app/
│   │   ├── components/
│   │   ├── hooks/
│   │   └── lib/
│   └── package.json
├── shared/                      # 共享代碼
│   ├── types/                   # 類型定義
│   ├── constants/               # 常數
│   └── sdk/                     # SDK 封裝
├── docs/
│   └── ai-disclosure/           # AI 使用披露（黑客松必需）
│       ├── prompts-log.md       # 原始 prompt 記錄
│       └── prompts-sanitized.md # 去敏版本（提交用）
└── .env.example                 # 環境變數範例
```

---

## 開發指令

### 合約
```bash
cd contracts
sui move build
sui move test
sui client publish --gas-budget 100000000
```

### Bot
```bash
cd bot
npm install
npm run dev      # 開發模式
npm run build    # 編譯
npm start        # 生產模式
```

### WebApp
```bash
cd webapp
npm install
npm run dev      # 開發模式 http://localhost:3000
npm run build    # 編譯
npm start        # 生產模式
```

---

## 安全機制

1. **非託管設計**：用戶資金存放在鏈上合約，Bot 無法挪用
2. **權限分離**：只有 Owner 可以提取資金，Keeper 只能執行預定交易
3. **滑點保護**：每筆交易設定最大滑點限制（預設 1%）
4. **緊急暫停**：用戶可隨時暫停或關閉倉位
5. **金額限制**：單筆交易設有上限防止意外損失
6. **重置保護**：Rebalance 設有最小間隔避免頻繁操作

---

## AI 使用披露規則（黑客松強制要求）

### 使用的 AI 工具
- **Claude Code CLI** (Anthropic)
- 模型版本：Claude Opus 4.5 (claude-opus-4-5-20251101)

### Prompt 記錄要求
1. 每次重要對話後，將 prompt 摘要記錄到 `docs/ai-disclosure/prompts-log.md`
2. 提交前製作去敏版本到 `prompts-sanitized.md`

### 去敏規則
將以下內容替換：
- API Keys → `[REDACTED_API_KEY]`
- 私鑰/助記詞 → `[REDACTED_SECRET]`
- 個人郵件 → `[REDACTED_EMAIL]`
- 內部 URL → `[REDACTED_URL]`
- Telegram Bot Token → `[REDACTED_BOT_TOKEN]`
- 錢包地址（如需保密）→ `[REDACTED_WALLET]`

---

## 注意事項
- ⚠️ 專案必須在 2026/1/27 之後創建（commit history 可驗證）
- ⚠️ 必須有可運行的 Web App，不接受只有代碼的提交
- ⚠️ 所有代碼必須開源
- ⚠️ AI 使用必須完整披露，否則取消資格

---

## 參考資源
- [Cetus Aggregator 文檔](https://github.com/CetusProtocol/aggregator)
- [Cetus CLMM SDK](https://github.com/CetusProtocol/cetus-clmm-sui-sdk)
- [stablelayer 文檔](https://docs.stablelayer.site/)
- [Sui Move 官方文檔](https://docs.sui.io/concepts/sui-move-concepts)
- [Sui SDK 文檔](https://sdk.mystenlabs.com/typescript)
- [Grammy Telegram Bot Framework](https://grammy.dev/)
